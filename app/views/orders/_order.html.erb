<tr>
  <td class="th-product"><%= order.id %></td>
  <td class="th-details"><%= order.created_at %></td>
  <th class="td-add-to-cart"><%= link_to t("orders.show.detail_label"), order_detail_path(order) %></th>
</tr>
